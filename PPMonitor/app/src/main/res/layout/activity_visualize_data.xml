<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_visualize_data"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Views.Activities.VisualizeDataView"
    app:theme="@style/My_Apptheme"
    android:background="@color/whiteColor">



    <android.support.v4.view.ViewPager
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:id="@+id/viewpager" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_below="@+id/viewpager"
        android:layout_marginBottom="10dp"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_alignParentEnd="true"
        android:gravity="center_vertical">
        <Button
            android:id="@+id/bleBtn"
            android:background="@drawable/shadowroundbtn"
            android:layout_height="wrap_content"
            android:layout_width="60dp"
            android:layout_gravity="center_vertical"
            android:text="Start recording"
            android:textColor="@android:color/white"
            android:textAlignment="center"
            android:textStyle="normal|bold"
            android:fontFamily="sans-serif-medium"
            android:textSize="9dp"
            android:onClick="onStartStopClick"/>
    </LinearLayout>

</RelativeLayout>
